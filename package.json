{
  "name": "pi-learning-starter",
  "version": "0.1.0",
  "private": true,
  "description": "五个 Agent 与一块黑板：基于 pi 的自学工作流扩展",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --test \"tests/**/*.test.ts\"",
    "check": "npm run typecheck && npm test",
    "due": "node scripts/due-check.mjs",
    "assess": "node scripts/assess-cron.mjs"
  },
  "engines": {
    "node": ">=22.6"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.84.2",
    "@earendil-works/pi-coding-agent": "^0.84.2",
    "@earendil-works/pi-tui": "^0.84.2",
    "@types/node": "^24.0.0",
    "typebox": "^1.3.7",
    "typescript": "^5.9.0"
  }
}
