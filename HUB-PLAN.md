# Hub 方案：常驻多实例与寻址制群聊

本方案把交互形态从「单会话内切换角色的工作流」改为「常驻多实例的灵活调用」：每个角色一个后端实例，独立对话用显式 `@` 唤醒，群聊按寻址制路由。治理层（黑板、bb_* 工具、闸门对话框、亲笔编辑器）原样保留——改变的是会话拓扑，不是规则与数据。

## 1. 目标与非目标

目标：

- 每个角色是一个可独立运行的后端实例（`pi --mode rpc` 子进程），有自己的持久会话；
- 独立对话：`@角色` 显式唤醒某个实例单聊；
- 群聊：多成员共享同一时间线；消息按寻址路由，只有被点名的实例产生回复；
- 会话内共享公共上下文：黑板（全体、持久）+ 群转写（会话级，注入被点名成员）；
- 路由只有一层：路由器 → 具体 Agent。Agent 的回复不触发其他 Agent。

非目标（当前明确不做）：

- Agent 之间互相触发、自由接话、多轮自主协商；
- 对 Agent 产出不满时的仲裁 / 顾问机制（advisor）——留待实际使用后再议；
- 替换黑板：它仍是唯一共享状态与唯一写入路径。

## 2. 已核对的 pi 约束（2026-08 对 0.84.2）

1. **RPC 传输是每进程一条 stdio JSONL 通道**，无端口占用；N 个实例并存没有资源冲突，扩展 UI 子协议（confirm / select / input / editor / notify / setStatus）按实例通道天然隔离。Obsidian 插件的 `PiRpcClient` 已实现该通道（含对话框回写），可直接实例化 N 份。
2. **RPC 命令集**：`prompt`、`steer`、`follow_up`、`abort`、`new_session`、`switch_session`、`fork`、`get_state`、`get_messages`、`get_entries`、`get_commands`、`set_session_name`、`set_model` 等。**没有「注入消息但不触发回合」的命令**，因此群转写不能经 RPC 推送，必须由扩展侧注入（见 §3.4）。
3. **固定角色**：实例以 `env.LEARN_ROLE=<role>` 启动（`PiRpcOptions.env` 已支持），扩展在 `session_start` 里应用该角色；会话文件按实例各自生成，互不冲突；`switch_session` 可在实例崩溃重启后恢复原会话。
4. **黑板没有跨进程写保护**：bb_* 工具的 `executionMode: "sequential"` 只在单实例内成立；多实例并发调用会在 JSON 读改写上互相覆盖。需要新增跨进程锁（§3.6）。

## 3. 架构

### 3.1 组成

```
Hub（Obsidian 插件升级；后续可抽成独立进程）
  ├─ 实例管理器：8 个角色实例（懒启动、崩溃重启、会话恢复）
  ├─ 路由器：@ 解析 → prompt 到目标实例；无 @ → 前台实例
  ├─ 群转写：落盘 .pi/group/<群id>.jsonl，供扩展注入
  ├─ UI 多路复用：各实例的对话框请求 → 模态框（标注来源角色）
  └─ 回合序列化：并发触发的实例回合排队执行（黑板写入的第一道防线）
每个实例 = pi --mode rpc（LEARN_ROLE 固定角色，工作目录同一项目）
共享层 = blackboard/（治理与持久状态）+ .pi/group/（会话级转写）
```

### 3.2 路由规则（寻址制，一层）

- `@角色 消息` → 该实例收到一次用户回合；`@A @B 消息` → 依次触发 A、B（串行，见 §3.6）；
- 不带 `@` 的消息 → 前台实例（解释处境、建议该找谁；它自己不代行专职角色）；
- Agent 回复中的 `@` 只作为文本呈现，不触发任何实例——路由权只在学习者手里；
- 角色名与别名（馆员 / librarian / 资料管理员…）由 hub 维护一张确定性映射表，解析是代码行为，不经模型。

### 3.3 独立对话与群聊的关系

两者是同一机制的两个视图：单聊 = 成员只有你和一个实例的群。hub 里每个实例保有自己的完整会话（pi 会话文件），群时间线是 hub 聚合各实例消息后的展示层。同一实例可以出现在多个群/单聊里，但其 pi 会话是连续的一条——群只是路由与转写的分组单位。

### 3.4 共享上下文

- **黑板**：不变，全体角色的持久共享状态；各角色上下文仍按 `buildContext` 从黑板装配。
- **群转写**：hub 把群内消息（发言者、寻址、正文摘录）落盘到 `.pi/group/<群id>.jsonl`；扩展新增一个注入器，在 `before_agent_start` 时把本实例所属群的转写尾部（截断策略：最近 N 条或 M 字符）作为自定义消息注入，复用 `learning-context` 的哈希去重，内容不变不重复注入。被点名者因此「看得到群里刚才发生了什么」，未点名者不消耗任何推理。
- **隔离策略**：提案评审员与复盘老师**不接收群转写注入**，它们的上下文永远只来自黑板——评审独立性与「只依据结构化数据」在群聊架构下从物理保证降为策略保证，此两条策略写死在扩展的注入器里（按角色过滤），不作为群配置暴露。

### 3.5 治理层保持不变

- 黑板写入仍只经 bb_* 工具；闸门对话框（接受提案、下载入库、核验、单元完成）经各实例通道弹出，hub 标注来源角色后渲染为模态框；
- 亲笔编辑器、闭卷作答、信心评分照旧；
- 提交类工具的尾部询问保留会话内可执行的项（接受、作答、写复盘）；跨角色的下一步（送评审）在 hub 模式下改为回复文案提示「可 @评审员」——与一层路由原则一致，不代替学习者点名；
- `nextSteps` 降级为前台实例的建议来源；`/learn`、`/go` 在每个实例内照常可用（终端单实例模式不受影响，两种形态并存）。

### 3.6 并发与锁

- 第一道防线：hub 串行执行回合——同一时刻只有一个实例在流式输出（多 `@` 依次触发）。简单、可预期，also 控制成本。
- 第二道防线：扩展内跨进程黑板锁——bb_* 工具执行前获取 `.pi/blackboard.lock`（`mkdir` 原子性 + 超时重试 + 陈旧锁回收），执行后释放。防御 hub 之外的并发（终端会话与 hub 并用、cron 出题撞上交互回合）。

### 3.7 自主运行（Phase 3）

自主性来自触发器而非拓扑：hub 监听 `events.jsonl` 与黑板变化，唤醒对应实例做**准备性工作**（馆员补选材提案、考评官到期出题），产物照旧落黑板排队等学习者裁决。触发的回合同样进串行队列；每类触发有冷却时间，避免自激。

## 4. 分期

| 阶段 | 内容 | 结束标志 |
| --- | --- | --- |
| P1 花名册与独立对话 | 实例管理器（懒启动 / 重启 / 会话恢复）、`@` 解析与别名表、每实例页签视图、UI 多路复用标注来源、黑板跨进程锁、回合串行队列 | 可以同时与馆员和陪读老师各聊各的，工作流感消失 |
| P2 群视图 | 聚合时间线、群转写落盘与扩展注入器（含评审员 / 复盘老师过滤）、多 `@` 顺序触发、转写截断策略 | 一条消息点名两个角色、其余成员可见上下文 |
| P3 自主触发 | 事件 watcher、准备性工作队列、冷却与静默时段 | 到期出题、缺口补料无人值守发生，裁决仍在人 |

advisor 机制预留在 P3 之后：对某实例产出不满时，把产出与不满说明作为黑板工件交给一个仲裁角色——届时仍走一层路由，不开 Agent 私聊。

## 5. 风险与开放问题

1. **隔离降级**：评审独立性从结构保证变为注入器里的策略（§3.4）；接受此让步是本方案的前提。
2. **成本**：常驻实例的上下文各自累积；寻址制加哈希去重压住大头，但总消耗高于单会话形态。P1 起在 hub 里显示每实例的上下文用量（RPC `get_session_stats` 可用）。
3. **崩溃恢复**：实例退出后 hub 用 `switch_session` 恢复原会话；黑板锁的陈旧锁需要超时回收。
4. **pi 升级**：RPC 协议与扩展 API 的兼容性随版本校验；`rpc-client.test.ts` 的真实 pi 测试是回归防线。
5. **开放**：群转写的截断参数、别名表的维护位置（`.pi/learning.json` 新增一段）、Obsidian 之外是否需要终端版 hub——P1 期间定。
